import {NS} from "/github/nsMockup.mjs"; main(new NS);

/** @param {NS} ns **/
export async function main(ns) {
    ns.tail();
    ns.clearLog();

    let contractList = ns.args[0];

    // Listen to the queue

    // Check if contract is still available

    // Identify contract type

    // Spawn the corresponding Sherlock solver

    // Loop
}